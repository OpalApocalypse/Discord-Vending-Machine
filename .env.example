DISCORD_TOKEN=replace-me

# Optional: register slash commands to one guild for faster development.
# Leave blank for global commands.
GUILD_ID=

# Optional: lets this Discord user run admin stock commands even without Manage Server.
OWNER_ID=

# Optional: where the editable machine stock/settings JSON is stored.
DATA_PATH=data/machine.json
